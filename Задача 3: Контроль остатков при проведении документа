Проблема: Запретить проведение документа "Реализация", если недостаточно товара на складе.
__________________________________________________________________________________________

// В модуле объекта документа "РеализацияТоваровУслуг":
&НаСервере
Процедура ПровестиПроверка(Отказ)
    Для Каждого Строка Из Товары Цикл
        Остаток = РегистрыНакопления.ОстаткиТоваров.Остатки(
            Строка.Номенклатура,
            Строка.Склад,
            Дата
        ).Получить(0);
        
        Если Остаток < Строка.Количество Тогда
            Сообщение = Новый СообщениеПользователю;
            Сообщение.Текст = "Недостаточно товара " + Строка.Номенклатура + 
                              " на складе! Остаток: " + Остаток;
            Сообщение.Сообщить();
            Отказ = Истина;
            Возврат;
        КонецЕсли;
    КонецЦикла;
КонецПроцедуры
