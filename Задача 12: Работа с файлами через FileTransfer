Проблема: При проведении документа сохранять PDF-файл в прикрепленные файлы.
____________________________________________________________________________

&НаСервере
Процедура ПослеЗаписи(Успешно)
    Если Не Успешно Тогда Возврат; КонецЕсли;
    
    ИмяФайла = "Документ_" + Объект.Номер + ".pdf";
    ДанныеФайла = СформироватьPDF();
    
    Файл = Новый ХранилищеЗначения(ДанныеФайла);
    ПрикрепленныеФайлы = Объект.ПолучитьПрикрепленныеФайлы();
    ПрикрепленныеФайлы.Добавить(Файл, ИмяФайла);
    Объект.УстановитьПрикрепленныеФайлы(ПрикрепленныеФайлы);
КонецПроцедуры
