Проблема: Изменить цвет строк в отчете при определенных условиях.
_________________________________________________________________
// В модуле отчета
&НаСервере
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки)
    // ... формирование отчета ...
    
    Для Каждого Область Из ДокументРезультат.Области Цикл
        Если ТипЗнч(Область) = Тип("ТабличныйДокументОбластьСтрок") Тогда
            Для Каждого Строка Из Область.Строки Цикл
                Если Строка[3] > 100000 Тогда // Если сумма > 100 000
                    Строка.ЦветФона = Новый Цвет(255, 200, 200);
                КонецЕсли;
            КонецЦикла;
        КонецЕсли;
    КонецЦикла;
КонецПроцедуры
